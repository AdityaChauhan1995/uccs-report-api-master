// processing functions for all adhoc reports
module.exports.processReport1 = function (data) {
    return new Promise(function (resolve, reject) {
        let status = true;
        if (status) {
            resolve({})
        } else {
            reject({})
        }
    });
}